{
  "permissions": {
    "allow": [
      "Bash(chmod +x test_ai.sh)",
      "Bash(chmod +x run.sh)",
      "Bash(ls -la *.md *.sh .env*)",
      "Bash(pip3 list)",
      "Bash(pip3 install langchain langchain-openai langchain-core)",
      "Bash(python3 -c \"from langchain_openai import ChatOpenAI; from langchain_core.prompts import ChatPromptTemplate; from langchain_core.output_parsers import StrOutputParser; print(''✅ LangChain imports successful'')\")",
      "Bash(python3 -c \"import fund; print(''✅ fund.py syntax check passed'')\")",
      "Bash(ls -la *.md)"
    ]
  }
}
